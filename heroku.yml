build:
  docker:
    web: Dockerfile
run:
  web: gunicorn --chdir ./recommender recommender.wsgi --bind 0.0.0.0:$PORT
release:
  image: web
  command:
    - python recommender/manage.py collectstatic --noinput
